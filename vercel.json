{
  "installCommand": "npm ci && npm --prefix backend ci && npm --prefix backend run prisma:generate",
  "buildCommand": "npm run typecheck:all && npm run build:all && npm --prefix backend run prisma:deploy && npm --prefix backend run prisma:seed",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/book",
      "destination": "/index.html"
    },
    {
      "source": "/staff",
      "destination": "/index.html"
    },
    {
      "source": "/schedule",
      "destination": "/index.html"
    }
  ]
}
