{
  "installCommand": "npm ci --include=dev && npm --prefix backend ci --include=dev && npm --prefix backend run prisma:generate",
  "buildCommand": "npm run typecheck:all && npm run build:all && npm --prefix backend run prisma:deploy && npm --prefix backend run prisma:seed",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index?__api_path=:path*"
    },
    {
      "source": "/book",
      "destination": "/index.html"
    },
    {
      "source": "/staff",
      "destination": "/index.html"
    },
    {
      "source": "/schedule",
      "destination": "/index.html"
    }
  ]
}
